export interface ProviderTableInterface {
    name: string,
    description: string,
    columns: string[],
    lisence: string,
    link: string,
    available: boolean //Estp dice si los datos se pueden visualizar o no
}
